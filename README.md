# git-scripts  
  
1. Clone folder to some location on your computer  
2. Add that path to system environment variables  
3. Use command as filename specifies omitting the dash (git-new becomes git new)